package domain

class Session(
    val user: User
) {
    val token = uuid()
    var expiry = now() + 30l * 24 * 60 * 60 * 1000
    static val tokenIdx = Index<string, Session>(true, s -> s.token)

    fn isValid() -> bool {
        return expiry > now()
    }
}
